(function installMaklomWebAwesomeControls(){
  'use strict';

  const ROOT_SELECTOR='#uploadView,#mergeView,#exportView,#duplicatesView,#formAttendanceView,#databaseTable,#eventsView';
  const TEXT_INPUT_TYPES=['text','email','number','search','tel','url','password'];

  if(!document.getElementById('event-row-assignment-style')){
    const sentinel=document.createElement('meta');sentinel.id='event-row-assignment-style';sentinel.dataset.externalStyles='webawesome-controls';document.head.appendChild(sentinel);
  }

  function rootFor(element){return element&&element.closest?element.closest(ROOT_SELECTOR):null;}
  function isEvents(element){return !!(element&&element.closest&&element.closest('#eventsView'));}
  function alreadyEnhanced(element){return !element||element.dataset.waEnhanced==='true'||element.dataset.waControlEnhanced==='true'||element.classList.contains('wa-proxy-source')||element.classList.contains('wa-control-source')||!!element.closest('wa-button,wa-input,wa-select,wa-textarea');}
  function makeId(source,prefix){return source.id?source.id+'ControlWa':prefix+'_'+Math.random().toString(36).slice(2,10);}
  function explicitLabel(source){if(!source.id)return null;return document.querySelector('label[for="'+source.id.replace(/"/g,'\\"')+'"]');}
  function wrappingLabel(source){const label=source.closest('label');return label&&label.contains(source)?label:null;}
  function prepareLabel(source,proxy){if(wrappingLabel(source))return;const label=explicitLabel(source);if(!label)return;const text=(label.textContent||'').trim();if(text)proxy.setAttribute('label',text);label.classList.add('wa-control-label-source');}
  function syncCommon(source,proxy){proxy.toggleAttribute('disabled',!!source.disabled);proxy.toggleAttribute('required',!!source.required);if(source.getAttribute('aria-label'))proxy.setAttribute('aria-label',source.getAttribute('aria-label'));if(source.getAttribute('aria-expanded')!==null)proxy.setAttribute('aria-expanded',source.getAttribute('aria-expanded'));if(source.title)proxy.title=source.title;if(source.placeholder)proxy.setAttribute('placeholder',source.placeholder);else proxy.removeAttribute('placeholder');}
  function copyInputLimits(source,proxy){['min','max','step','maxlength'].forEach(function(name){const value=source.getAttribute(name);if(value!==null)proxy.setAttribute(name,value);else proxy.removeAttribute(name);});}
  function buttonVariant(button){if(button.classList.contains('danger'))return'danger';if(button.classList.contains('primary')||button.classList.contains('active'))return'brand';return'neutral';}
  function buttonAppearance(button){return button.classList.contains('primary')||button.classList.contains('danger')||button.classList.contains('active')?'filled':'outlined';}
  function syncButton(button){const proxy=document.getElementById(button.dataset.waControlProxy||'');if(!proxy)return;syncCommon(button,proxy);proxy.setAttribute('variant',buttonVariant(button));proxy.setAttribute('appearance',buttonAppearance(button));if(button.classList.contains('small'))proxy.setAttribute('size','small');else proxy.removeAttribute('size');proxy.classList.toggle('is-active',button.classList.contains('active'));const text=(button.textContent||'').trim();if(proxy.textContent!==text)proxy.textContent=text;}
  function enhanceButton(){return;}
  function syncInput(source){const proxy=document.getElementById(source.dataset.waControlProxy||'');if(!proxy)return;syncCommon(source,proxy);copyInputLimits(source,proxy);if(proxy.value!==source.value)proxy.value=source.value;}
  function enhanceInput(source){if(!source||!rootFor(source)||alreadyEnhanced(source))return;const type=(source.type||'text').toLowerCase();if(TEXT_INPUT_TYPES.indexOf(type)===-1)return;const proxy=document.createElement('wa-input');proxy.id=makeId(source,'waControlInput');proxy.className='maklom-wa-control-field';proxy.setAttribute('size','small');proxy.setAttribute('type',type);if(['text','search','email','tel'].indexOf(type)>-1)proxy.setAttribute('with-clear','');prepareLabel(source,proxy);source.dataset.waControlEnhanced='true';source.dataset.waControlProxy=proxy.id;source.classList.add('wa-control-source');source.parentNode.insertBefore(proxy,source);syncInput(source);proxy.addEventListener('input',function(){source.value=proxy.value;source.dispatchEvent(new Event('input',{bubbles:true}));});proxy.addEventListener('change',function(){source.value=proxy.value;source.dispatchEvent(new Event('change',{bubbles:true}));});source.addEventListener('input',function(){syncInput(source);});source.addEventListener('change',function(){syncInput(source);});new MutationObserver(function(){syncInput(source);}).observe(source,{attributes:true,attributeFilter:['disabled','required','placeholder','min','max','step','maxlength','aria-label']});}
  function rebuildSelect(source){const proxy=document.getElementById(source.dataset.waControlProxy||'');if(!proxy)return;const value=source.value;syncCommon(source,proxy);proxy.replaceChildren();Array.from(source.options||[]).forEach(function(option){const item=document.createElement('wa-option');item.value=option.value;item.textContent=option.textContent;item.toggleAttribute('disabled',!!option.disabled);proxy.appendChild(item);});proxy.value=value;}
  function enhanceSelect(source){if(!source||!rootFor(source)||alreadyEnhanced(source))return;const proxy=document.createElement('wa-select');proxy.id=makeId(source,'waControlSelect');proxy.className='maklom-wa-control-field';proxy.setAttribute('size','small');prepareLabel(source,proxy);source.dataset.waControlEnhanced='true';source.dataset.waControlProxy=proxy.id;source.classList.add('wa-control-source');source.parentNode.insertBefore(proxy,source);rebuildSelect(source);proxy.addEventListener('change',function(){source.value=proxy.value;source.dispatchEvent(new Event('change',{bubbles:true}));});source.addEventListener('change',function(){rebuildSelect(source);});new MutationObserver(function(){rebuildSelect(source);}).observe(source,{attributes:true,childList:true,subtree:true,characterData:true});}
  function syncTextarea(source){const proxy=document.getElementById(source.dataset.waControlProxy||'');if(!proxy)return;syncCommon(source,proxy);const max=source.getAttribute('maxlength');if(max!==null)proxy.setAttribute('maxlength',max);else proxy.removeAttribute('maxlength');if(proxy.value!==source.value)proxy.value=source.value;}
  function enhanceTextarea(source){if(!source||!rootFor(source)||alreadyEnhanced(source))return;const proxy=document.createElement('wa-textarea');proxy.id=makeId(source,'waControlTextarea');proxy.className='maklom-wa-control-field';proxy.setAttribute('size','small');proxy.setAttribute('resize','vertical');prepareLabel(source,proxy);source.dataset.waControlEnhanced='true';source.dataset.waControlProxy=proxy.id;source.classList.add('wa-control-source');source.parentNode.insertBefore(proxy,source);syncTextarea(source);proxy.addEventListener('input',function(){source.value=proxy.value;source.dispatchEvent(new Event('input',{bubbles:true}));});proxy.addEventListener('change',function(){source.value=proxy.value;source.dispatchEvent(new Event('change',{bubbles:true}));});source.addEventListener('input',function(){syncTextarea(source);});source.addEventListener('change',function(){syncTextarea(source);});new MutationObserver(function(){syncTextarea(source);}).observe(source,{attributes:true,attributeFilter:['disabled','required','placeholder','maxlength','aria-label']});}
  function enhance(root){const scope=root&&root.querySelectorAll?root:document;if(scope.matches){if(scope.matches('input'))enhanceInput(scope);if(scope.matches('select'))enhanceSelect(scope);if(scope.matches('textarea'))enhanceTextarea(scope);}scope.querySelectorAll('input').forEach(enhanceInput);scope.querySelectorAll('select').forEach(enhanceSelect);scope.querySelectorAll('textarea').forEach(enhanceTextarea);}
  function start(){enhance(document);new MutationObserver(function(mutations){mutations.forEach(function(mutation){mutation.addedNodes.forEach(function(node){if(node.nodeType===1)enhance(node);});});}).observe(document.body,{childList:true,subtree:true});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
