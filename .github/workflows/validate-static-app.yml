name: Validate static app

on:
  pull_request:
    paths:
      - 'assets/**'
      - 'src/**'
      - 'scripts/**'
      - 'index.html'
      - 'package.json'
      - 'vercel.json'
      - '.github/workflows/validate-static-app.yml'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check legacy JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          find assets -type f -name '*.js' -print0 | sort -z | while IFS= read -r -d '' file; do
            echo "Checking $file"
            node --check "$file"
          done
      - name: Run event report import tests
        run: node tests/event-report-import-core.test.js
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Build static app
        run: npm run build
      - name: Verify Web Awesome build output
        shell: bash
        run: |
          set -euo pipefail
          test -f dist/index.html
          test -f dist/assets/webawesome-ui.js
          test -f dist/assets/webawesome-ui.css
          test -f dist/assets/webawesome-controls.js
          test -f dist/assets/webawesome-controls.css
          test -f dist/assets/webawesome-overlays.js
          test -f dist/assets/webawesome-overlays.css
          test -f dist/assets/webawesome-dialogs.js
          test -f dist/vendor/webawesome/webawesome.loader.js
          test -f dist/vendor/webawesome/styles/webawesome.css
          grep -q 'vendor/webawesome/webawesome.loader.js' dist/index.html
          grep -q 'assets/webawesome-ui.js' dist/index.html
          grep -q 'assets/webawesome-controls.js' dist/index.html
          grep -q 'assets/webawesome-overlays.js' dist/index.html
          grep -q 'assets/webawesome-dialogs.js' dist/index.html
          grep -q 'assets/event-report-import-core.js?v=20260921-1' dist/index.html
          grep -q 'assets/event-report-import.js?v=20260921-1' dist/index.html
          grep -q "async function createVolunteerIdentity" assets/event-report-import.js
          grep -q "vol_event_report" assets/event-report-import.js
          ! grep -q "retained without a profile link" assets/event-report-import.js
          ! grep -q 'assets/webawesome-events.js' dist/index.html
          ! grep -q 'assets/webawesome-event-forms.js' dist/index.html
          ! grep -q 'assets/webawesome-static-workflows.js' dist/index.html
          ! grep -q 'assets/webawesome-workflows.js' dist/index.html
          ! grep -qi 'mantine' dist/index.html
          grep -q 'assets/webawesome-ui.js?v=20260921-2' dist/index.html
          grep -q 'assets/webawesome-controls.js?v=20260921-1' dist/index.html
          ! grep -q "scope.querySelectorAll('button').forEach(enhanceButton)" assets/webawesome-ui.js
          ! grep -q "scope.querySelectorAll('button').forEach(enhanceButton)" assets/webawesome-controls.js
          ! grep -A6 'const observer=new MutationObserver(function(mutations)' assets/webawesome-ui.js | grep -q 'syncAllFields();'
          grep -q 'refreshPromise:null' assets/shared-db/core.js
          grep -q 'if(S.state.refreshPromise)return S.state.refreshPromise' assets/shared-db/core.js
          grep -q 'if(st.remoteRefreshPromise)return st.remoteRefreshPromise' assets/shared-db/sync.js
          grep -q 'assets/shared-db/core.js?v=20260921-1' dist/index.html
          grep -q 'assets/shared-db/sync.js?v=20260921-1' dist/index.html
          grep -q 'aria-label="About MakLom"' dist/index.html
